import React, { Fragment } from 'react';
import { BrowserRouter as Router, Route, Switch } from 'react-router-dom';

// react components

// scss

const App = () => {
  return (
    <Router>
      <Fragment>
        <div></div>
      </Fragment>
    </Router>
  );
};

export default App;
